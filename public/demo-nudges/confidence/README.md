Place MP3 files for the "confidence" mood here.

Naming suggestions:
- confidence-1.mp3
- confidence-2.mp3

The app will prefer files in `/demo-nudges/confidence/` for the "confidence" mood. If the folder is empty, it falls back to `/demo-nudges/confidence.mp3` in the root.